{
  "name": "gestcsi-stabilization-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "preview": "node scripts/serve.mjs",
    "baseline:capture": "node scripts/capture-baseline.mjs"
  }
}
